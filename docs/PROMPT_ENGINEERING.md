# SynapseIQ — Enterprise AI Prompt Engineering & Schema Specification

This document details the prompt engineering design, McKinsey/BCG persona constraints, domain adaptation logic, and JSON schema contracts powering **SynapseIQ's Intelligence Engine**.

---

## 🎯 1. Core AI Persona & System Constraints

All prompts generated by SynapseIQ enforce strict executive advisory persona constraints:

```typescript
Role: Executive Business Advisor (McKinsey / BCG persona).
Guidelines:
1. Speak concisely, analytically, and authoritatively. Avoid generic or repetitive phrasing.
2. Ground all insights in local biAnalysis metrics (covariance, slopes, Z-scores).
3. Focus on trends (slopes, MoM vectors) and covariance between metrics instead of isolated numbers.
4. Every strategic recommendation MUST output a stringified JSON object conforming to the NodeRecommendation contract.
```

---

## 🏭 2. Industry-Adaptive Context Directives

SynapseIQ dynamically detects the dataset's business domain during CSV preprocessing and injects specialized industry directives:

| Industry Domain | Key Focus Directives | Primary Strategic Vectors |
|---|---|---|
| **Sales & Revenue** | Commercial Strategy | Revenue growth velocity, ARR expansion, territorial margin optimization |
| **Human Resources** | Workforce Intelligence | Retention risk mitigation, performance-compensation alignment, flight risk isolation |
| **Finance** | Financial Optimization | Operating cost reduction, cash flow conversion, net profit conservation |
| **Healthcare** | Clinical Operations | Patient throughput, readmission SLA reduction, treatment cost efficiency |
| **Manufacturing** | Yield & Supply Chain | Predictive maintenance, downtime elimination, defect rate reduction |
| **Retail / E-Commerce** | Inventory Velocity | Stock turnover, gross margin conservation, customer acquisition efficiency |
| **Education** | Academic Outcomes | Student retention, graduation rate elevation, faculty allocation |
| **Logistics** | Freight & Distribution | Route fuel efficiency, delivery time SLA protection, distance optimization |
| **Banking** | Risk Management | Credit default mitigation, deposit-to-loan ratios, capital liquidity |
| **Energy** | Sustainability | Downtime minimization, efficiency ratings, carbon footprint reduction |

---

## 📋 3. JSON Output Schema Contracts

Every Google Gemini API request mandates strict JSON output schemas:

### A. Executive Analysis Schema Contract (`generateGeminiAnalysis`)
```json
{
  "nodeContexts": {
    "health": {
      "id": "health",
      "title": "Business Health Index",
      "summary": "...",
      "metricLabel": "...",
      "metric": "...",
      "trend": "up|down|neutral",
      "opportunity": "...",
      "risk": "...",
      "recommendation": "{\"recommendation\": \"We recommend...\", \"businessReasoning\": \"...\", \"supportingMetrics\": \"...\", \"expectedImpact\": \"...\", \"confidenceScore\": \"...\", \"potentialRisks\": \"...\", \"implementationDifficulty\": \"Low|Medium|High\", \"priority\": \"Low|Medium|High\", \"suggestedTimeline\": \"...\"}"
    }
  },
  "businessSignals": [],
  "briefingReports": [],
  "strategyCanvasEdges": [],
  "timelineEvents": []
}
```

### B. Decision Copilot Schema Contract (`askGeminiCopilot`)
```json
{
  "summary": "Executive Summary & Business Context...",
  "evidence": ["Key Finding 1...", "Root Cause 2..."],
  "confidence": 95,
  "recommendation": "STRINGIFIED_RECOMMENDATION_JSON",
  "nextQuestion": "Suggested follow-up query starter..."
}
```

---

## 🔄 4. Model Negotiation & Fallback Protocol

SynapseIQ implements an automated multi-tier model negotiation fallback chain (`callGeminiRaw`) to guarantee 99.99% uptime and zero disruption during API rate limits (HTTP 429), model deprecations, or service unavailability (HTTP 503):

```
┌─────────────────────────────────────────────────────────────┐
│ 1. gemini-2.5-flash (Primary High-Speed Reasoning Model)    │
│    └─► HTTP 429 / 503 / 404 Fallback                      │
│ 2. gemini-2.0-flash (Secondary Scenario Simulation Model)   │
│    └─► HTTP 429 / 503 / 404 Fallback                      │
│ 3. gemini-2.0-flash-lite (Ultra-Low Latency Streaming)     │
│    └─► HTTP 429 / 503 / 404 Fallback                      │
│ 4. gemini-1.5-flash (Legacy Baseline Fallback Model)        │
│    └─► Network Loss / No API Key                          │
│ 5. Local Strategy Engine (Offline Client Heuristic Engine)  │
└─────────────────────────────────────────────────────────────┘
```

### Rate-Limiting & Exponential Backoff
- **Exponential Backoff**: Initial retry delay of 1,000ms scaling with $2^{\text{attempt}-1}$ up to 3 total attempts per endpoint request.
- **Rate-Limit Guard (HTTP 429)**: Automatically multiplies backoff delay by $3\times$ when rate-limit quotas are detected.
- **Aborting Support**: Full integration with browser `AbortSignal` for instant cancellation of pending HTTP requests when navigating between views.


---

## 🛡️ 5. Validation & Sanitization Loop

Before any Gemini response hydrates the Zustand global store, it passes through `cleanAndValidateAnalysis()` / `cleanAndValidateCopilot()`:

1. **Repetition Check**: Rejects responses with duplicate sentences.
2. **Phrase Filtering**: Rejects responses containing prohibited generic filler phrases (*"Based on the data..."*, *"As an AI language model..."*).
3. **JSON Structure Audit**: Ensures required keys exist and nested JSON recommendations parse cleanly.
4. **Auto-Regeneration**: If validation fails, the service auto-retries up to 2 times before falling back to local heuristic calculations.
